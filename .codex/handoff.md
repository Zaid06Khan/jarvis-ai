# Session Handoff — 2026-06-21

On 2026-06-21 we restored access and verified the live Jarvis system rather than rebuilding it: SSH now works as `deploy@159.203.3.38` on **port 2222** (root/port-22 disabled; a `~/.ssh/config` alias makes plain `ssh 159.203.3.38` work), and all services were confirmed running on the VPS — the FastAPI router v2.3.0 on :8000 with all 22 agents, the Hermes Discord gateway, n8n, redis, the 2am overnight n8n workflow, and the Vercel dashboard. Graphify was verified on the VPS graph (`/opt/edvisingu/graphify-out/`), a local deep-mode graph was built (41 nodes), and the Graphify Claude Code integration was installed with a PreToolUse hook in `.claude/settings.json`. CLAUDE.md was fully updated with this verified status, the SSH/port-2222 details, the dual-graph routing rule, and the Claude Code (VPS/agents/architecture) ↔ Codex (UI/boilerplate/fast code) division of work. **Pick up here:** the TikTok automation pipeline for **@BuildingWithAI** is planned but not built — all keys exist (HeyGen, Blotato, ElevenLabs, DALL-E) and it's a wiring job (`hermes-tiktok` script → HeyGen video → Blotato post → n8n schedule → Discord approval), but it is blocked on four operator scope decisions captured in `.codex/tasks.json` (video format, approval flow, first-build scope, topic source). The recommended starting point is **faceless video + Discord approval + one end-to-end test video + hermes-research topics**. No pipeline code has been written yet.
